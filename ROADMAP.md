# henge — roadmap

Forward direction as sized milestones. Sizes: S ≤ 1 session, M = 1–2,
L = 2–4.

- [ ] **M0 — Mission (S).** Owner writes `MISSION.md`: what henge is, the
      sacred invariants, the non-goals. Nothing else here can be sized until
      this lands, and the repo stays at Level 3 until it does.
- [ ] **M1 — First domain type (S).** Replace the `SemanticVersion` scaffold
      in `HengeCore` with the real first type from the mission, and its tests.
- [ ] **M2 — First screen (S–M).** Replace the placeholder `RootView` with
      something that shows real state on both platforms.
